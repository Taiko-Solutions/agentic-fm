# Taiko Knowledge Base Manifest

Taiko-specific architectural patterns and development decisions. These documents define how Taiko Solutions builds FileMaker solutions — they complement the general FileMaker knowledge in `agent/docs/knowledge/`.

All paths are relative to `agent/docs/taiko/knowledge/`.

---

| File | Description | Keywords |
|------|-------------|----------|
| `clew-pattern.md` | Error handling system using Loop/Exit Loop If as pseudo try-catch; error.* custom functions; errorTrace JSON propagation; error constants; validation patterns; **namespace vs hint distinction**; cross-script hint propagation pattern; **Response Envelope** uniforme (`error.GetCategory`, `error.GetResponse`) para Controllers de borde consumidos por sistemas externos (Node.js, MCP, Data API/OData, dialogs FM); convención `$$DEBUG_MODE` para incluir/omitir trace; mapeo de categorías (validation, not_found, conflict, business_rule, internal); constante de versión `_clew_VERSION`. XML source: `agent/docs/taiko/custom_functions/clew.xml` (43 functions) | error, try, catch, clew, error handling, throw, exit loop, error.Throw, error.WasThrown, error.GetTrace, error.GetCode, error.GetHint, error.GetDescription, error.GetScriptName, error.InSubscript, error.InSubscriptThrow, error.ThrowIf, error.ThrowIfLast, error.CreateVarsFromKeys, error.ThrowIfMissingParam, error.ThrowIfMissingVar, error.ThrowIfWrongContext, error.DeleteTrace, error.Config, error.PrivateBuildObject, error.PrivateGetConfig, error.PrivateSetTrace, error.GetCategory, error.GetResponse, _clew_VERSION, errorTrace, _error_, json.IsValid, json.IsErrorTrace, json.CreateVars, json.ArrayAddToFront, Log.Entry, Log.Data, Log.Reset, getScriptEnvironment, IsRunningOnServer, namespace, hint, $ErrorHint, response envelope, ok true, ok false, $$DEBUG_MODE, debug mode, category, validation, not_found, conflict, business_rule, internal, uniform response, JSON envelope, API response, MCP response, Controller de borde |
| `clew-subscript-propagation.md` | **Usar cuando** un Controller de borde llama a subscripts NO transaccionales y el envelope debe conservar la `category`/`code`/`hint` reales del hijo (el error SÍ llega por `Get(ScriptResult)`; vs `clew-transactional-dual.md`, que es para transacciones con `Revert` que matan al hijo). Propagación de errores entre subscripts Clew: **usar `error.InSubscript` (no `InSubscriptThrow`)** para que un Controller de borde refleje en el Response Envelope la categoría/código/hint REALES del subscript; `InSubscriptThrow` envuelve todo como `internal/UNEXPECTED`. Precondición: el subscript devuelve `errorTrace` crudo (patrón clásico), no el envelope. Agregadores que llaman a otros Controllers de borde parsean `ok`/`data` a mano y degradan. Caso real: `task.list`→`_resolve.user`/`_resolve.project`; `daily.panel`. | InSubscript, InSubscriptThrow, propagación, subscript, Perform Script, error.GetResponse, errorTrace, UNEXPECTED, categoría, code, hint, _resolve, agregador, daily.panel, GetAsBoolean ok, json.IsErrorTrace, PrivateSetTrace, helper reutilizable |
| `clew-transactional-dual.md` | **Usar cuando** el subscript entra en una transacción con `Revert` que mata al hijo (`Get(ScriptResult)` llega vacío → la global es el único canal; vs `clew-subscript-propagation.md`, que es para envelope semántico sin transacción de por medio). Patrón canónico de script transaccional Clew **dual padre/hijo** con propagación robusta de errores vía variable global `$$ClewError` y 4 custom functions `Clew.SetError`/`Clew.ClearError`/`Clew.HasError`/`Clew.GetError`. Resuelve el problema del `Revert Transaction` anidado que mata al hijo antes de su `Exit Script`. Soporta **N niveles** de anidamiento. Detecta estado con `Get(TransactionOpenState)` e `$IsRoot`. Template de Worker (+ Orchestrator equivalente con loop de subscripts y propagación post-Perform Script). `Revert Transaction [Condition: Clew.HasError]` + `Commit Transaction` lineal obligatorio (el linter rechaza Commit dentro de If). Captura `Clew.GetError` en `$ErrorTrace` antes de `Clew.ClearError` en root. `Commit Records/Requests` defensivo antes de `New Window` para evitar error 301 de locks cross-ventana. Validado con batería de 6 casos en `Clew | Runner Pruebas`. Complementa `utility-transactional.md` (sin UI — flujo transaccional puro) | clew, transactional, dual, padre, hijo, anidado, nested, $$ClewError, Clew.SetError, Clew.ClearError, Clew.HasError, Clew.GetError, Open Transaction, Commit Transaction, Revert Transaction, IsRoot, TransactionOpenState, N niveles, recursión, error 301, Commit Records, record lock, rollback, propagación errores, Template Worker, Template Orchestrator, Runner Pruebas, pseudo try-catch, Loop, Exit Loop If, _error_UNEXPECTED, $ErrorTrace |
| `three-layer-architecture.md` | Three-layer architecture: Interface (UI, no data changes), Controller (business logic, transactions, Go to Layout), Data (tables, fields, relationships); unidirectional flow; anti-patterns | architecture, layer, interface, controller, data, separation, responsibility, Go to Layout, .Controller, business logic, three layer, 3 capas |
| `utility-transactional.md` | Non-destructive editing pattern with global fields in a Utility table, modal card window, and transactional controller with automatic rollback; Utility Manager, Initialize Shadow, and Transactional scripts; **error 3 handling in parent/child transactions**; Commit error check with `not error.WasThrown` guard; unconditional Revert in child scripts; parent-only logging; **$$TRANSACTION_ERROR pattern for nested transaction error propagation**; `transaction.SetError`/`transaction.GetHint`/`transaction.Clear` custom functions; **Revert del hijo salta al Commit del padre**; ErrorCode/ErrorMessage obligatorios en Revert; pre-captura de hint; captura de LastError+LastErrorDetail en un solo Let(); integración con `getScriptEnvironment` y Create Log | transaction, utility, global fields, card window, modal, rollback, revert transaction, open transaction, commit transaction, non-destructive, edit, create, shadow, Utility Manager, Initialize Shadow, AsJSON, error 3, TransactionOpenState, error.WasThrown, parent, child, subscript, $$TRANSACTION_ERROR, transaction.SetError, transaction.GetHint, transaction.Clear, nested transaction, error propagation, ErrorCode, ErrorMessage, LastErrorDetail, getScriptEnvironment, pre-capture, hint |
| `logging-system.md` | Error logging to a Log table with Create Log Clew script; optional transaction logging to Registro table; retry capability from logged records; Geist-to-Clew normalization for cross-framework compatibility | log, logging, error log, registro, retry, reintentar, Create Log Clew, error.NormalizeFromGeist, Geist, transaction log |
| `trigger-suppression.md` | Trigger suppression pattern using TriggersAreActive/TriggersDisable/TriggersEnable; triggered scripts must check TriggersAreActive at the top; scripts that navigate layouts or perform operations that fire triggers should wrap with TriggersDisable/TriggersEnable; **Pattern 4: migration/batch scripts must wrap the whole body with TriggersDisable/TriggersEnable, with TriggersEnable placed BEFORE `If [error.WasThrown]` in the catch block so it always runs (success or error)**. XML source: `agent/docs/taiko/custom_functions/triggers.xml` | trigger, triggers, TriggersAreActive, TriggersDisable, TriggersEnable, TriggersReset, OnLayoutEnter, OnModeEnter, OnRecordLoad, suppress, bypass, script trigger, Go to Layout, migration, batch, post-deploy, $! bit-bucket |
| `executesql-pattern.md` | Resilient ExecuteSQL pattern using SQL.GetFieldName() and SQL.GetTableName() to dynamically resolve names; prevents breakage on field/table renames; Let/Substitute template with placeholders; anti-patterns. **⚠ `SQL.RecordExists` upstream bug: always returns True — NO USAR**; replace with `not IsEmpty ( SQL.GetColumn(…) )` or `SQL.GetColumn2Fields(…)`. XML source: `agent/docs/taiko/custom_functions/sql-cfs.xml` | ExecuteSQL, SQL, SQL.GetFieldName, SQL.GetTableName, SQL.GetColumnStatement, SQL.GetColumn, SQL.GetColumn2Fields, SQL.GetRecordsAsJSON, SQL.RecordExists, bug, siempre True, idempotencia, record exists, query, SELECT, WHERE, Substitute, fm-sql-cfs, Char(29), Char(30), separador de fila, separador de columna, parseo multi-fila, trailing control char, recorte caracteres de control, variable, PatternCount, columna centinela, eor, última fila perdida, fila de menos, array JSON, FilterValues |
| `cross-to-pointer-in-loops.md` | In loops that cross base tables via `Go to Layout`, the cross-TO record pointer is unreliable — capture source values to `$*` variables BEFORE switching layouts, then write target fields from variables only. Observed in a post-deploy migration where all N target records copied data from the first source record because the cross-TO read resolved to record #1 instead of the iteration's current one. | cross-TO, puntero, pointer, loop, multi-layout, Go to Layout, Go to Record, migration, unrelated access, capture to variables, post-deploy |
| `json-defensive-patterns.md` | Defensive JSON handling patterns for FileMaker: JSONGetElement returns literal "null"; JSONSetElement with JSONNumber breaks on non-numeric values; JSON boolean true/false conversion to FileMaker values; optional numeric field handling; multi-pair JSONSetElement failure cascade. | JSON, JSONGetElement, JSONSetElement, JSONNumber, JSONString, null, boolean, true, false, GetAsNumber, defensive, API, IA, integration |
| `transaction-navigation.md` | Navigation strategies within open transactions: Perform Find cannot locate uncommitted records; auto-enter deferred until Commit; preserve position pattern; navigate found set; collect before leaving; ScriptResult communication; Set Revert Transaction on Error for controlled errors; anti-patterns and quick reference table. | transaction, Perform Find, New Record, navigate, found set, Go to Record, auto-enter, Commit Transaction, Open Transaction, Revert Transaction, Set Revert Transaction on Error, error 401, ScriptResult, index, uncommitted, temporary |
| `list-single-arg-gotcha.md` | **`List()` con UN solo argumento devuelve `?`** en soluciones Taiko: existe una custom function `List` de aridad ≥2 que tapa la nativa. El `?` es silencioso (fmlint no lo ve) y se propaga a funciones que lo envuelven. Fix: usar el literal de cadena directo (`$Var = "valor"`) o `List ( "valor" ; "" )`. Detectado en task.list (974 Bloque 6): `$VistasStub = List("bloqueadas")` → `"?"` → FilterValues no encajaba → vista stub mal clasificada. Incluye tabla de comportamiento verificado y la técnica de depuración con **AGFMEvaluation** (evaluar calcs en el motor FM en vivo desde Python) + nota del **doble envoltorio de AGFMScriptBridge**. | List, List single argument, un solo argumento, un argumento, aridad, arity, signo de interrogación, question mark, "?", custom function tapa nativa, shadow native function, FilterValues, ValueCount, enum validation, lista de un valor, AGFMEvaluation, evaluar calc en vivo, live calc evaluation, AGFMScriptBridge, doble envoltorio, double envelope, resultParameter, debugging, gotcha |
| `superpowers-workflow.md` | Proceso de desarrollo (Superpowers) para tareas de **alcance estructural** en FileMaker: brainstorm → plan → casos de aceptación → implementar/verificar → review. Define el umbral (cuándo aplica); verificación por **casos de aceptación** con `script-test` (sustituye al TDD red-green, que no aplica en FM); debug vía `systematic-debugging` + `Skill-Debug`; mapeo de artefactos al vault. Complementa el contrato corto en `.claude/CLAUDE.md` (sección "Metodología de desarrollo — Superpowers") | superpowers, workflow, metodología, proceso, feature, funcionalidad nueva, diseño, spec, plan, módulo, refactor grande, casos de aceptación, brainstorming, writing-plans, requesting-code-review, systematic-debugging, Skill-Debug, script-test, alcance estructural, review, verificación, alcance, nueva feature |
| `combined-workflow.md` | Metodología **combinada**: cómo agentic-fm (autoría de scripts/cálculos/esquema vía fmxmlsnippet/OData) + Superpowers (proceso) + ProofKit (Vía 1 MCP = ver en vivo; Vía 3 Web Viewer = interfaces web, motor por defecto) funcionan como un todo. El bucle unificado (entender → diseñar/plan → implementar → verificar → cerrar), el árbol de decisión de qué herramienta usar, cuándo **proponer una interfaz web**, y el gating por `connectedFiles` con degradación elegante al flujo estático. Complementa `agent/docs/taiko/fm-access.md` (las 3 vías) y `agent/docs/taiko/proofkit/`. | proofkit, proof kit, mcp, web viewer, webviewer, interfaz web, react, typegen, data api, data_api_orchestrator, execute_filemaker_sql, layout_metadata, display_erd_diagram, deploy_html, setup_proofkit_project, connectedFiles, 3 vías, tres vías, fm-access, combinada, integración, agentic-fm, superpowers, ver en vivo, construir UI, motor web por defecto, proponer interfaz, gating, degradación elegante, bucle unificado |
